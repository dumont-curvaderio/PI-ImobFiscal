# Perfil ativo padrão — sobrescrito por variável de ambiente em produção
# Para rodar localmente: perfil dev é ativado automaticamente
spring.profiles.active=dev

# ─── JPA (compartilhado) ──────────────────────────────────────────────────────
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# ─── SERVIDOR ────────────────────────────────────────────────────────────────
server.port=8080

# ─── JWT (compartilhado — valor sobrescrito por variável de ambiente em prod) ─
jwt.secret=aW1vYmZpc2NhbC1zZWNyZXQta2V5LXBhcmEtZmF0ZWMtcGktMjAyNg==
jwt.expiration=86400000
